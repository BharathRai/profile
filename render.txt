Deploying resume using render : 

-created a separate repository
-tap on deploy a web service in render
-using following =>  build command -- pip install fastapi "uvicorn[standard]"
		     start command -- uvicorn app:app --host 0.0.0.0 --port $PORT
-web deploy